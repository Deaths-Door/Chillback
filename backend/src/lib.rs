uniffi::setup_scaffolding!();

// TODO : Maybe make all this using an api , so that it doesn't matter about all the code i need to install + maybe less need for bindings and hence lower size and compile time
mod youtube;
mod database;